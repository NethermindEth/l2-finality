import { SxProps, Theme } from '@mui/material/styles'

export const averageStatsPaperStyle: SxProps<Theme> = {
  padding: 2,
  backgroundColor: 'var(--light-background-color)',
  boxShadow: '0px 4px 8px rgba(0, 0, 0, 0.1)',
  borderRadius: '8px',
  maxWidth: '80%',
  margin: 'auto',
}
